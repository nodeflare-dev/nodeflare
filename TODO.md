# TODO（自分でやること）

メモリ選択＋従量課金のコードは実装済み。本番で動かすために残っている手作業だけ。

## 1. デプロイ時：DBマイグレーション適用
次回デプロイで自動適用される（手動でDBに流していない）。確認だけ。
- `20240139000000_add_runtime_oom_hint.sql` … OOM時のヒント
- `20240140000000_add_server_memory.sql` … サーバーごとのメモリ
- `20240141000000_add_region_usage_gb_minutes.sql` … 従量課金の計測

## 2. 従量課金をONにする（Stripe側）
今は **計測だけ動いていて課金はされない**。下記をやると課金が始まる。

- [ ] Stripeで **Pro用の従量(metered/usage-based)price** を作成
  - 単価 **¥4/GB時間** がおすすめ
  - **最初の250GB時間を¥0** にする段階(graduated)tierを設定 ＝ Proの「込み枠」
- [ ] `.env` に設定
  - [ ] `STRIPE_PRICE_PRO_USAGE=price_xxx`（↑で作ったprice id）← これを入れると課金ON
  - [ ] `USAGE_RATE_JPY_PER_GB_HOUR=4`（ダッシュボードの概算表示用）
- [ ] **Stripeテストモードで動作確認**してから本番へ（コードはテスト不可なので必須）

## 3. 既存Proユーザーへの対応（未実装）
- 新規加入者は checkout で自動的にメーターitemが付く。
- **既存のProサブスクには付かない** → そのままだと既存ユーザーは課金されない。
- 必要なら「既存サブスクにメーターitemを追加する処理」を実装依頼すること。

## 4. 単価・アロウワンスの最終決定（任意）
- 推奨：¥4/GB時間 ＋ 込み250GB時間/月。
- 変えるなら 2 の price と env を変更するだけ。

---
※ メモリ階層（256/512/1024/2048）・プラン上限（Free=256, Pro=2048）・OOMヒントは実装済みで設定不要。
